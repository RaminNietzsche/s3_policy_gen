# Agents

Persian docs: `docs/content/` · build: `make docs` · site: [policygen.ceph-s3.ir](https://policygen.ceph-s3.ir)

Rules: no secrets in git; credentials only on localhost + `server.py`; Arvan product naming in UI; `js/cloud-data.js`, `js/app.js`, `deploy/config.py`.
