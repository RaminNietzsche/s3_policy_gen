# کپی: cp .deploy.env.example .deploy.env
# برای make deploy-quick هر سه کلید زیر الزامی است.
# .deploy.env در gitignore است — secret را commit نکنید.

S3_BUCKET=policygen
S3_ENDPOINT=https://s3.ir-thr-at1.arvanstorage.ir
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
