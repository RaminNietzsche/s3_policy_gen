# استقرار

مستندات کامل: **[policygen.ceph-s3.ir/deploy.html](https://policygen.ceph-s3.ir/deploy.html)**

```bash
make install && make assets && make deploy
```

ویرایش منبع: `docs/content/deploy.md` · ساخت: `make docs`
