# سیاست امنیتی

## نسخه‌های پشتیبانی‌شده

رفع آسیب‌پذیری روی آخرین شاخهٔ `main` انجام می‌شود.

## گزارش آسیب‌پذیری

اگر مشکل امنیتی دیدید (XSS در خروجی، نشت credential، سوءاستفاده از پروکسی و …):

1. Issue عمومی GitHub با جزئیات exploit یا **کلید واقعی** باز نکنید.
2. از طریق Security Advisory خصوصی GitHub یا کانال امن معمول خود گزارش دهید.

## فرض‌های طراحی

| موضوع | رفتار |
|--------|--------|
| **Secretها** | Access/Secret فقط در **حافظهٔ تب مرورگر** در حالت توسعهٔ لوکال. در `localStorage` ذخیره نمی‌شوند (فقط اختیاری: Access Key + کلاستر تا بستن تب). |
| **Deploy استاتیک** | سایت عمومی S3 برای وارد کردن کلید production **مناسب نیست**. فیلد کلید تا وقتی `server.py` روی localhost تشخیص داده نشود غیرفعال است. |
| **`.deploy.env`** | در gitignore. هرگز commit نشود. فقط برای `make deploy` لوکال. |
| **`/s3-proxy`** | فقط توسعهٔ لوکال؛ درخواست‌های امضاشده را فوروارد می‌کند. `server.py` را بدون فهم ریسک به اینترنت expose نکنید. |

## مسئولیت کاربر

Policy و CORS اشتباه می‌توانند دسترسی را قطع یا داده را expose کنند. قبل از اعمال روی bucket تولید، خروجی را بخوانید.
