<div dir="rtl" align="right">

# سیاست امنیتی

## نسخه‌های پشتیبانی‌شده

رفع آسیب‌پذیری برای آخرین شاخهٔ `main` انجام می‌شود.

## گزارش آسیب‌پذیری

لطفاً جزئیات آسیب‌پذیری یا کلیدهای واقعی را در Issue عمومی منتشر نکنید. از Security Advisory خصوصی GitHub یا کانال امن سازمانی استفاده کنید.

## اصول طراحی

| موضوع | رفتار |
|--------|--------|
| **کلیدها** | در توسعهٔ لوکال فقط در حافظهٔ تب مرورگر؛ Secret در `localStorage` ذخیره نمی‌شود |
| **سایت استاتیک** | ورود کلید production روی URL عمومی مجاز نیست |
| **`.deploy.env`** | در gitignore؛ commit نشود |
| **`/s3-proxy`** | فقط محیط لوکال؛ بدون آگاهی به اینترنت expose نشود |

## مسئولیت کاربر

پیکربندی نادرست Policy یا CORS می‌تواند دسترسی را محدود یا داده را در معرض خطا قرار دهد. پیش از اعمال در محیط تولید، خروجی را بازبینی کنید.

</div>
